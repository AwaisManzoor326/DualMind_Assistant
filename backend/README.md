
# DualMind Assistant Backend

FastAPI backend for DualMind Assistant.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
2. Set up `.env` with your Hugging Face API Token.
3. Run the server:
   ```bash
   uvicorn app.main:app --reload
   ```
